{
  "name": "auth-react",
  "version": "1.0.0",
  "description": "Full-stack authentication application with Node.js, PostgreSQL, Sequelize and React",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && node index.js",
    "client": "cd client && npm run dev",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["auth", "react", "nodejs", "postgresql", "sequelize"],
  "author": "goncharrov@yahoo.com",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}